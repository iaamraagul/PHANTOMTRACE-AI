# Data

This directory contains lightweight schemas and safe sample fixtures only.

Large official datasets are downloaded into `ml/data/raw/` by `scripts/download_data.py` and should not be committed.
