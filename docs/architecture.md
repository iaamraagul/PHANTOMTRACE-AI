# Architecture

`React/Vite → FastAPI → canonical feature extractor → calibrated model (after training) → transparent risk engine → SQLite scan history`.

No submitted URL crosses a network boundary. Threat intelligence is a separate provider adapter with failure-tolerant status results. The frontend uses API data only and does not render URLs as HTML.
